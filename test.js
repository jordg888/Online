(function() {
    console.log('🚀 ТЕСТ: скрипт запустився');
    
    // Простий alert
    alert('Плагін завантажено!');
    
    // Функція яка спрацює через 5 секунд
    setTimeout(function() {
        console.log('⏰ 5 секунд минуло');
        alert('5 секунд минуло - скрипт працює!');
    }, 5000);
    
    // Слідкуємо за карткою
    Lampa.Listener.follow('card', function(event) {
        console.log('📺 Подія card:', event.type);
        if (event.type == 'render') {
            alert('Картка відкрилась!');
        }
    });
    
    console.log('✅ ТЕСТ: скрипт виконано');
})();
